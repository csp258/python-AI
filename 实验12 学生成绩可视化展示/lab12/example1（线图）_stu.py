import numpy as np
import matplotlib.pyplot as plt
import math
# 生成数据
x_values = np.arange(0.0, math.pi * 4, 0.01)  # 0到4π，步长0.01
y_values = np.sin(x_values)                   # 计算sin(x)
# 绘制图形
  # 绘制图像
  # 添加x轴标签
  # 添加y轴标签
  # 设置图标题
plt.rcParams['font.sans-serif'] = ['Microsoft YaHei']  # 设置中文字体（避免乱码）
  # 显示图例
  # 保存图片
  # 显示绘图对象






